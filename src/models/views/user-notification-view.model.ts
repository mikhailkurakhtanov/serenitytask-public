import { UserNotification } from "../entities/user-notification.model";

export class UserNotificationView {
    userNotification: UserNotification;
    senderName: string;
    senderAvatar: string;
}